# POS frontend production environment for pos.hiddenoasis.app.
# Copy to /etc/hiddenoasis/pos-frontend.env and use the same values when running npm run build.

NODE_ENV=production
PORT=3200
NEXT_PUBLIC_API_BASE=/api
