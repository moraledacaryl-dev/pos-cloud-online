# POS Deployment

Use the exact Hidden Oasis live deployment guide:

```text
/opt/accounting-program-online/docs/HIDDENOASIS_LIVE_DEPLOYMENT.md
```

The live POS target is:

```text
https://pos.hiddenoasis.app
```

The live deployment path is PostgreSQL, nginx, HTTPS, and systemd.
