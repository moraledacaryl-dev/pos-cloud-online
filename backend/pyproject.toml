[tool.ruff]
target-version = "py312"
line-length = 140
exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
ignore = [
  "E701",
  "E702",
  # SQLAlchemy intentionally overloads equality to build SQL expressions.
  "E711",
  "E712",
]
